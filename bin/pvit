#!/bin/bash

here=$(me=$(readlink --canonicalize $0); cd ${me%/*}; pwd)
tmp=$(mktemp)
${here}/vit \
    --cmd "profile start ${tmp}" \
    --cmd "profile func *" \
    --cmd "profile file *" \
    $@ \
&& vim ${tmp} +/SORTED
