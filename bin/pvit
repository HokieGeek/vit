#!/bin/bash

tmp=`mktemp`
vim \
    --cmd "profile start ${tmp}" \
    --cmd "profile func *" \
    --cmd "profile file *" \
    --cmd "let g:vit_standalone=1" \
    -c "Git log "$1 \
    -c "Git status" \
    -c "wincmd t" \
    $@ \
&& vim ${tmp} +/SORTED
