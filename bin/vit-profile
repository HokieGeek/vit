#!/bin/bash

tmp=`mktemp`
vim \
    --cmd "profile start ${tmp}" \
    --cmd "profile func *" \
    --cmd "profile file *" \
    -c "Git log "$1 \
    -c "Git status" \
    $@ \
&& vim ${tmp} +/SORTED
