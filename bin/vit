#!/bin/bash

[ "$1" == "-h" -o "$1" == "--help" ] && {
    echo "USAGE: `basename $0` [file]" >&2
    exit
}

git rev-pasre --is-inside-work-tree >/dev/null 2>&1 || {
    echo "Not inside a git working tree" >&2
    exit 1
}

exec vim -c "Git log "$1 \
         -c "Git status" \
         $@
