#!/bin/bash

usage() {
    {
    [ $# -gt 0 ] && echo -e "${1}\n"
    echo "USAGE: `basename $0` [file]"
    } >&2
}
[ "$1" == "-h" -o "$1" == "--help" ] && usage

# if [ $# -gt 0 ]; then
    # exec vim $@ -c "Git log"
# else
    exec vim -c "set nonumber" -c 'if exists("&relativenumber")|set norelativenumber|endif' \
        -c "call vit#PopGitFileLog('"$1"')"
# fi
