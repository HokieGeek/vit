#!/bin/bash

[ "$1" == "-h" -o "$1" == "--help" ] && {
    echo "USAGE: `basename $0` [file]" >&2
    exit
}

exec vim --cmd "let g:que__vcs_section_enabled = 0" \
         --cmd "let g:vit_log_use_new_colors = 1" \
         -c "silent! %bwipeout" \
         -c "call vit#PopGitFileLog('"$1"')"
